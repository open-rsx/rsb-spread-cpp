SET(RSBYARP_RSBYARP_PACKAGE_VERSION "@RSBYARP_VERSION@")
 
# Check whether the requested PACKAGE_FIND_VERSION is compatible
IF("${RSBYARP_PACKAGE_VERSION}" VERSION_LESS "${PACKAGE_FIND_VERSION}")
    SET(RSBYARP_PACKAGE_VERSION_COMPATIBLE FALSE)
ELSE()
    SET(RSBYARP_PACKAGE_VERSION_COMPATIBLE TRUE)
    IF("${RSBYARP_PACKAGE_VERSION}" VERSION_EQUAL "${PACKAGE_FIND_VERSION}")
        SET(RSBYARP_PACKAGE_VERSION_EXACT TRUE)
    ENDIF()
ENDIF()
    